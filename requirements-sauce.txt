# us-hot-sauce-corpus 的相依（與 Scout 既有的 requirements.txt 分開，互不影響）。
#
# evdb 以可編輯模式從隔壁 repo 裝進來：不 vendor、不 fork、不改它一行（A3、A4）。
# 它自己帶 duckdb / pyarrow / PyYAML，所以這裡不重複列。
-e ../1-github/evdb

# 驗收與測試
pytest>=8

# 憑證信任鏈：Python 在 Windows 的預設信任庫會拒絕一些瀏覽器照收的鏈（見 sauce/net.py）
truststore>=0.10
certifi

# 得獎名錄是 PDF（Scovie）；只讀文字，不做 OCR、不處理圖片
pypdf>=4

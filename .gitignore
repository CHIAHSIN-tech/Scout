# 機密設定（絕對不能上傳）
# checklist/ 與 scout-checklist/ 兩個資料夾已不存在，規則保留當防呆——
# 萬一有人在別台機器還原成舊佈局，config.js 一樣進不了版控。
.streamlit/secrets.toml
secrets.toml
checklist/config.js
scout-checklist/config.js
buylist/config.js
web/config.js
web/_buylist-body.htmlpart
test_gemini.py

# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/

# 資料庫本地檔案
*.db
*.db-shm
*.db-wal
*.sqlite

# 系統檔案
.DS_Store
Thumbs.db

# 編輯器 / 工具
.claude/
.cursor/
.vscode/

# wrangler dev 的本機狀態（快取、模擬的儲存空間），純暫存
.wrangler/

# MCP 設定含 Supabase key，只留 .example 進版控
.mcp.json
mcp-server/.env

# ── 旅程資料：等 repo 轉私有就把這一整段刪掉 ──
#
# 已決定要進版控（DECISIONS-trip-page.md 的 D13 選項 B），**但前置還沒完成**：
# repo 目前是公開的（gh api repos/CHIAHSIN-tech/Scout --jq .private → false），
# 而 trip.json 含 booking_ref／旅館地址／班機時刻。
# 順序不能反——公開時進版控，一旦推上去就進 git 歷史，拿不回來。
#
# 改可見性需要 admin，Stanley 只有 push，**只有 Chia 做得到**
# （for-chia-cloudflare.md 步驟 0.5）。
#
# 她改完之後：確認 `gh api repos/CHIAHSIN-tech/Scout --jq .private` 回 true，
# 然後刪掉這一整段、`git add trips/ web/trips/`，就完成了。
trips/
# build_trip_page 的產出，內容與 trip.json 相同（只是變成 HTML）。
# 只擋 trips/ 而不擋這裡，等於從後門把同一份資料推上去，所以一起擋、一起放。
web/trips/

# 最終報告 — us-hot-sauce-corpus（v3）

> 驗收表逐條 PASS/FAIL 與**原始輸出**在 [`ACCEPTANCE-sauce-corpus.md`](ACCEPTANCE-sauce-corpus.md)。
> 對照 Stanley 原話的三份清單在 [`REQUEST_CHECK-sauce-corpus.md`](REQUEST_CHECK-sauce-corpus.md)。

## 0. 這一輪：49 PASS / 1 FAIL / 1 BLOCKED

依 spec 的規則，任何一項 FAIL 或 BLOCKED，整個 run 就是失敗。剩下的兩條都說得出為什麼：

| 項目 | 判定 | 為什麼 | 誰能解 |
|---|---|---|---|
| A17 | FAIL | 召回率 0.784，門檻 0.90。**但 16 筆未命中裡有 11 筆的品牌已經在總表上**——probe 寫 `Original Red Sauce / Tabasco`，總表上那瓶叫 `Tabasco® brand pepper sauce`。量到的是下界（K25） | 換一份用真實品名的 probe 清單（貨架照片或 buylist），不是放寬比對 |
| A32 | BLOCKED | 從空的 `.evdb` 照作業書跑到底這一輪沒有做（一次三小時以上的重跑） | 下一輪自然會驗到 |

上一次是 41/49，這一輪是 49/51。中間 Stanley 改了兩件事，
而那兩件事把原本卡住的五條全部解開了。

## 1. 改的是什麼

### 1.1 評論：留連結，不留評論內容（D27）

> 「我們要留的不是 review 的文字，是要留 links……就是每一個 Hot Sauce 的
> Reference, not the review itselves」、「only keeps aggregated reviews」

**這一改把整條評論線上的模型拿掉了。** 原本要模型讀完一篇評論抽出立場／分數／
代表句，那有三個問題：代表句選得好不好沒有任何機器檢查蓋得到；它要過冷啟動人審關卡；
**而且我們其實不需要它**——要回答「這款醬有誰評過」，一個連結就夠。

彙整型的判準不需要分類器：**就是「這篇對上了幾個相異的醬名」**。

收乾淨花了三輪，每一輪都是同一種錯的不同長相：

| 擋掉的 | 怎麼被發現的 |
|---|---|
| 撞名的品名 | `buffalo wing` 是 44 個品牌共用的品名——一篇 Texas Pete 身世故事提一次，掛上 44 款 |
| 短標題＋商品名 | 店家商品頁的「相關商品」側欄，一頁對上 12 個名字 |
| 自己賣辣醬的站 | 前十名裡有四篇是店家商品頁；白名單早就記著 `sells_products` |

### 1.2 判讀要跟獨立來源交叉驗證（D29）

> 「OCR 內容要跟 research results 再交叉驗證」

拿**同一個 GTIN 在 OFF 上的 `ingredients_text`** 比詞的回收率與精確率。
這一條立刻抓到兩個規則檢查抓不到的錯：模型把營養標示插進成分表；
三筆零分的其實是非英文標籤。

## 2. 這一輪實際收到什麼

| | 數量 | 對比上一輪 |
|---|---|---|
| 產品 | **7,186** | — |
| 買得到的 | **1,861** | — |
| 品牌 | **2,803** | — |
| 標籤照片 | **451 張** | 145 → 451 |
| **標籤逐字判讀** | **387 筆** | **0 → 387** |
| 成分來自**實物標籤** | **363 列** | **0 → 363** |
| 成分來自 FDC 文字 | 1,135 列 | — |
| **來源不一致（兩值都留）** | **59 列** | **0 → 59** |
| 辣度上界算得出來 | **953**（57 筆 unbounded） | 777 → 953 |
| **出處連結** | **445 筆 / 212 款 / 54 篇彙整型** | 新增 |
| 代工聚類 | **61 組** | 50 → 61 |

**交叉驗證：回收率中位數 1.0、精確率中位數 1.0、覆蓋率 90.4%。**

## 3. 本地 OCR 取代不了視覺模型（量過，D28）

Stanley 問能不能用專用的圖轉文模型先萃取再處理。**架構上那就是現在的做法**
（模型只做逐字 → 存 JSON → 規則算欄位）。真正的問題是能不能換成零 token 的本機 OCR。
這台機器上有 Tesseract 5.4，所以直接量：

| | 視覺模型 | Tesseract |
|---|---|---|
| 解析得出成分表 | 20/20 | 14/20 |
| 平均成分項數 | 10.6 | 6.2 |
| 詞庫外比例 | 27.3% | **74.4%**（門檻 8%） |

瓶身是圓的、字是彎的、對比又低。**而且失敗的方式最糟**：14 張還是會通過
`looks_like_ingredients`，讀出來的是營養標示的碎片但長得像成分表。

## 4. 這一輪修掉的、原本看不見的錯

1. **冷啟動閘變成擺設**（D32）。pilot 連三次紅在「golden 一致率 11%」。
   三個 bug 疊在一起：golden 重播時**圖片不見了**（只留 `input`）；`promote` 又把
   `item` 丟掉；逐字比對對自由文字是錯的門檻。修完：**schema 失敗 0、
   一致率 95%、p95 從 56 秒掉到 10 秒**。
   差點把它當成「模型太小」而去換 90B——實測 11B 對同一張圖讀兩次的自我一致度
   是 0.94–1.00，一直都是穩的。
2. **GTIN 沒補零**（D33）。OFF 是 13 位、比對事件是 14 位，109 筆判讀一筆都接不回
   產品。而它失敗的樣子是靜悄悄的：只顯示 `label_photo: 0`。
3. **詞庫只有 214 個詞**，撐不住 109 份真實成分表（OOV 22.8%）。
   從 **OFF 的 128 萬筆獨立成分文字**裡挑出現 ≥200 次的詞擴到 8,835 個——
   **不拿我們自己的轉錄當依據**，那等於讓模型自己認證自己。OOV 降到 **1.58%**。
4. **A23 被自己的 prompt 觸發**：v3 起我們確實在「transcribe」——把標籤照片轉成字。
   pattern 收窄成真正的語音轉文字工具。

## 5. 沒有任何自動檢查蓋得到的兩件事

### 5.1 名字讀不讀得懂

`"Original Hot Sauce 5oz 2pk Value"` 通得過所有機器檢查，但它不是一個人會拿來
稱呼那瓶醬的說法。試樣裡的 40 筆產品就是要看這個。

### 5.2 成分的**順序**

交叉驗證比的是詞的集合。**詞一樣、順序顛倒，回收率與精確率都是 1.0**——
而美國標籤的成分是由多到少排的，「水排第一」跟「辣椒排第一」是兩種不同的產品。
這個缺口只有人看得出來（K22）。

## 6. 產物

| 檔 | 是什麼 |
|---|---|
| `sauce/out/2026-09-20/sauce_catalog-v1.csv` | 產品總表 7,186 列（含成分、辣度五層、出處連結） |
| `sauce/out/2026-09-20/sauce_buyable-v1.csv` | 買得到的 1,861 列 |
| `reports/sauce-label-crosscheck.md` | 判讀 × OFF 成分文字，逐筆回收率／精確率 |
| `reports/sauce-copackers.md` | 代工聚類 61 組，每個成員都附證據 |
| `reports/sauce-label-oov.md` | 判讀的詞庫外詞彙 |
| `ACCEPTANCE-sauce-corpus.md` | 驗收表 ＋ 每一條的原始輸出 |
| `DECISIONS-sauce-corpus.md` | 33 條決策 |
| `KNOWN_ISSUES-sauce-corpus.md` | 25 條限制，每條都寫「從哪裡看得出來」 |

**標籤照片一張都不進版控**：CC BY-SA 3.0，而且授權不涵蓋包裝上的商標與設計（D20）。

## 7. 下一輪最值得做的三件事

1. **換一份用真實品名的 probe 清單**——A17 現在量到的是「我們有沒有抓到」
   加上「probe 的寫法跟商品標示一不一樣」，後者跟覆蓋率無關。
2. **重跑時用「重放既有 snapshot」而不是重抓**（K20）。`event_id` 是內容的雜湊、
   `observed_at` 算在內容裡，重抓會換掉每一筆的 id。
3. **成分順序沒有機械守門員**（K22）。試樣裡那 20 筆標籤判讀是唯一會看到它的地方。

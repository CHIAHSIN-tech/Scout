# KNOWN ISSUES — 旅程頁模組 ＋ Cloudflare 遷移

日期：2026-09-12 ｜ 規格：`specs/spec-scout-trip-page.md`

> 主觀疑慮、以及「A-item 全過但仍然可能不好用」的缺口記在這裡。
> 可由命令檢測的問題不在這份檔案裡——那些會直接讓驗收 FAIL。
> 架構決策在 `context.md` §4，執行期決策在 `DECISIONS-trip-page.md`。

---

## 🔴 會影響使用的

### K1. 行程表目前不會跟著網站部署

`trips/` 與 `web/trips/` 都在 `.gitignore` 裡（repo 是公開的，`trip.json` 含訂位編號、
旅館地址、班機時刻）。所以：

- 建置產物只存在於**執行者的那台機器**。
- 線上的行程 Tab 底部會顯示「還沒有建置過的行程表」空狀態。
- A15「部署路徑不可猜」的機制做好了，但暫時沒有東西可部署。

**要用的話現在怎麼辦：** 直接開本機的 `trips/<slug>/index.html`，
或把那個檔案用任何方式傳到手機（它是自含單檔，複製過去就能開）。

**已決定的解法（2026-09-12，見 `DECISIONS-trip-page.md` D13）：選項 B**——
**repo 轉私有**、`trips/` 進版控、網站照樣公開部署，行程表另外用
**Cloudflare Access（Google SSO）擋住 `/trips/*`**。購物的分享連結維持公開，不受影響。

**🚧 卡在哪：改 repo 可見性需要 admin，Stanley 只有 push——只有 Chia 做得到**
（`for-chia-cloudflare.md` 步驟 0.5）。**順序不能反**：repo 還公開時把行程資料進版控，
一旦推上去就進 git 歷史拿不回來。所以 `.gitignore` 現在還擋著，**不是選了 A，
是 B 的前置沒完成**。

**她改完之後**：刪掉 `.gitignore` 最後那段、`git add trips/ web/trips/`，
K1 就解掉了——行程表跟著自動部署走，不必有人手動 deploy。

### K2. 手機可讀性沒有實測過，只驗了溢位規則

規格 INTERVIEW Q4 決定不用 Playwright（理由：ADR-010 零 build step，
引入 Playwright 要 node_modules 與新的 CI 前提）。A10 是**靜態規則**：
表格有沒有包在可捲動容器裡、有沒有卡片化規則、有沒有超過 360px 的固定寬度。

**驗不到的：** 字級夠不夠大、行距會不會太擠、中韓文混排會不會斷行怪、
地圖標籤疊在一起的地方看不看得懂。

**怎麼解：** `trips/2026-09-kr-seoul/REVIEW.md` 第 26–30 條就是為此存在的，
要 Stanley 在真手機上開過一次。

### K3. 地圖標籤在地點密集的區域仍然會擠

已經做了避讓（同一區的標籤往下推，只挪文字不挪點），首爾那趟從「整團糊住」
變成「看得懂但還是有點擠」。蠶室（四個地點在同一棟樓）與汝矣島（兩個地點同座標）
是最擠的兩處。

**為什麼不做得更好：** 參考產品那份地圖之所以乾淨，是因為它**一個節點代表一個「區域」**
（手工挑的），而不是一個地點。自動化要做到那個程度得先有「地點屬於哪個區域」的分群，
那是新功能不是修 bug。

**怎麼解（下一趟）：** `places[].area` 已經有了，可以用它把同一區的地點在地圖上合併成
一個節點、點開才展開。

---

## 🟡 設計上的取捨，知道就好

### ~~K4. 自動產生的待確認有噪音~~ ✅ 2026-09-13 已解

schema 加了 `kind: "area"`（開放街區、公園、河濱這種沒有門的地點）。
標成 area 的地點：**不自動發問、頁面不印「尚未查證」、排程也不檢查它的營業時間**。

為什麼是新增分類而不是用既有欄位判斷：真正的分界是「有沒有門」，
而 `shopping` 同時涵蓋 The Hyundai（有營業時間）與弘大商圈（沒有），
`sight` 同時涵蓋水族館與石村湖。用既有 kind 猜一定會猜錯一邊。

首爾這趟八個地點改標成 area，待確認 **19 筆 → 11 筆**（2 筆人工 ＋ 9 筆自動），
剩下的都是真的有門、有營業時間的地方。三個測試釘住這件事
（`test_unverified.py` 的 `test_open_air_areas_*`）。

### ~~K5. 時間軸上沒有「回旅館」那一段~~ ✅ 2026-09-13 已解

首爾這趟的 D2／D3 補了 `place_id: "hotel"` 的收尾 event 與對應的 leg
（21:30 回 Oriens、21:40 回 Oriens 順路逛明洞 Olive Young）。
頁面現在看得到「這天幾點回到旅館」，`check_schedule` 的夜間動線檢查也拿得到最後一站。

**這是資料的問題不是程式的問題**——之後每建一趟都要記得補收尾那一筆，
沒有程式擋得住忘記（要擋的話得加一條「每天最後一項必須回旅館」的規則，
但那對「最後一天直接去機場」就不成立，所以刻意不加）。

### K6. 排隊候補不能在地圖上定位

規格的「延後」清單第四項。候補清單本身有，但點卡片不會跳到地圖位置。
參考產品那段 JS 在 `trips/_reference/body2.part`，要補時直接移植。

### K7. 營業時間的解析只吃 `HH:MM–HH:MM`

`check_schedule` 從自由文字裡抽營業區間，只認得 `12:00–15:00` 這種格式。
韓文的「매일 11:30 ~ 21:00」抽得到，但「午餐時段」「打烊前一小時 L.O.」抽不到。
**抽不到時當「查不到」而不是「全天營業」**——這個方向是對的（保守），
但代價是有些其實查得到的店仍然被列進 `unverified`。

### K8. 沒有 CI

五支既有腳本 ＋ 四支新腳本 ＋ pytest 全部要人記得跑。
這是 `context.md` §6.4 就記著的既有技術債，本次沒有處理。

---

## ⚪ 環境／一次性的

### ~~K9. 本機 8788 埠被佔住~~ ✅ 2026-09-13 已解

換 session 之後埠就放開了。2026-09-13 的驗收**已經用規格原本指定的 8788 跑過**，
A21／A22 六項全過，不再需要 `--live=<port>`。

### K10. `wrangler` 版本落後

本機是 4.110.0，最新是 4.131.1。升版之後 `compatibility_date` 可以往後推
（目前釘在 4.110.0 支援的最新日期 2026-07-15）。不急，但換機器時要注意
`compatibility_date` 不能比當地的 wrangler 新。

---

## 外部相依（不是 bug，是還沒做的人工步驟）

2026-09-12 的 ADR-020 把基礎設施權限收回 Stanley，所以這張表比原本短很多——
**大部分本來要等 Chia 的事，現在 Stanley 自己就能做。**

### 只有 Chia 做得到（`for-chia-handover.md`）

| 項目 | 為什麼只有她 | 擋住什麼 |
|---|---|---|
| 把 repo 轉回 `witsper-stanley` | GitHub 的轉移只有擁有者能發起 | **K1**——轉移＋轉私有之前，旅程資料不進版控 |
| 把行程 Supabase `uarkccyqcqvgxukjcrey` 轉給 Stanley | 專案在她的 organization | Stanley 仍然改不了行程側的欄位（ADR-009 的老問題） |
| 停用 Netlify 站台 | 站台在她的帳號 | 沒擋住什麼，但要等 Cloudflare 跑穩才做 |

### Stanley 自己做（`runbook-cloudflare.md`）

| 項目 | 卡在哪 |
|---|---|
| 建 Cloudflare Worker、部署 | 沒有，現在就能做 |
| Cloudflare Access（Google SSO）擋 `/trips/*` | 要先部署完 |
| `GEMINI_API_KEY` 設成 Worker secret | 可先跳過（ADR-017 已讓網頁 AI 預設隱藏）|
| 重發家人的分享連結 | 要先部署完（網址才會確定）|
| `REVIEW.md` 第 26–30 條 | 要在真手機上看過 |

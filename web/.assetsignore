# ⚠️ 暫時性：Cloudflare Access 還沒設好之前，行程表不上傳。
#
# 為什麼需要這個檔：wrangler deploy 會上傳整個 [assets] directory（也就是 web/），
# 其中 web/trips/<page_slug>/index.html 含訂位編號、旅館地址、班機時刻。
# 而 Access 必須綁主機名，**要先有網站才設得起來**——所以第一次部署如果不排除，
# 行程表會在登入保護生效前先公開一段時間，只靠 6 碼路徑擋。
#
# 設好 /trips/* 的 Access 之後：刪掉下面那一行，再 deploy 一次，行程表就上去了。
# 步驟見 runbook-cloudflare.md 的「第一次部署」。
trips/
